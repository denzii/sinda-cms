@model PageProps
@{
    ViewData["Title"] = @Model.Name;
    int i = 0;
}

@foreach (PageTab tab in @Model.Tabs){
        <div class="@(i == 0 ? "main__section" : "main__section main__section--hidden")" data-key=@tab.Name>
            @if(tab.Status == SectionStatus.Incomplete) {
                await Html.RenderPartialAsync("../Shared/_Incomplete.cshtml", tab);
            } else{
                await Html.RenderPartialAsync($"../{@Model.Name}/_{tab.Name}.cshtml", tab);
            } 
        </div>
    i++;
}

